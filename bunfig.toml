[test]
# Pin the SQLite runtime before any test file opens a connection. See
# src/platform/persistence/test-preload.ts for why this cannot be left to import order.
preload = ["./src/platform/persistence/test-preload.ts"]
