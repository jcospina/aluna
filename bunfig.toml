[test]
# Pin the SQLite runtime before any test file opens a connection. See
# src/persistence/test-preload.ts for why this cannot be left to import order.
preload = ["./src/persistence/test-preload.ts"]
